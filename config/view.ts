import { ViewConfig } from '@adonis/core/views'

const viewConfig: ViewConfig = {
  views: 'resources/views',  // El directorio donde se almacenan las vistas
  engine: 'ejs',  // Especifica que usaremos EJS como motor de plantillas
}

export default viewConfig
